/**
 * Jednotný Vercel projekt: serverless vstup pre celý Express (trasy pod `/api/...`).
 * Lokálne API: `cd backend && npm run dev` (port 3001).
 */
import app from '../backend/server.js';

export default app;
